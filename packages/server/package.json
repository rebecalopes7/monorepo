{
  "name": "@monorepo/server",
  "version": "1.7.4",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "git:version": "git add ./package.json && git commit -m'chore(server): atualiza versao' && git push -u origin",
    "minor": "echo \"Atualiza versão para pacote server\" && yarn version --minor --no-git-tag-version",
    "patch": "echo \"Atualiza versão para pacote server\" && yarn version --patch --no-git-tag-version"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9"
  }
}
