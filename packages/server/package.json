{
  "name": "@monorepo/server",
  "version": "1.4.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "git:version": "git add ./package.json && git commit -m'chore(server): atualiza versao' && git push -u origin",
    "minor": "yarn config set version-git-message 'chore(server):atualiza versao' && yarn version --minor",
    "patch": "yarn config set version-git-message 'chore(server):atualiza versao' && yarn version --patch"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9"
  }
}
